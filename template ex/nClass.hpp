#ifndef NCLASS_HPP
# define NCLASS_HPP

class nClass {

	public:
		// CONSTRUCTOR DESTRUCTOR ASSIGNMENT 
		nClass ();
		nClass (nClass const& original);
		virtual ~nClass ();
		nClass& operator= (nClass const& toBecome);

		// GETTER

		// MEMBER FUNCTIONS

};


# endif